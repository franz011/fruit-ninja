const config = {
    game_settings: {
        width: 500,
        height: 888,
        time: 60 // seconds
    },
    renderer_options: {
        antialiasing: false,
        transparent: false,
        resolution: window.devicePixelRatio,
        autoResize: true,
        forceCanvas: true
    }
}

export default config;
